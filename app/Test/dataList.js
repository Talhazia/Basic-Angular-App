'use strict';

describe('myApp.dataList module', function() {

  beforeEach(module('myApp.dataList'));

  describe('dataList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dataList_Controller = $controller('dataListController');
      expect(dataList_Controller).toBeDefined();
    }));

  });
});
