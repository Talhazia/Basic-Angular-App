'use strict';

describe('myApp.details module', function() {

  beforeEach(module('myApp.details'));

  describe('details controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dataListDetails_Controller = $controller('dataListDetailsController');
      expect(dataListDetailsController).toBeDefined();
    }));

  });
});
